# Sajilo Sewa Nepal — Bootstrap

This is the initial bootstrap branch for the repository. The first feature PR
(Sajilo Sewa Nepal) is opened against this branch. Once merged, mark this
branch (or a renamed `main`) as the default branch in repository settings.
